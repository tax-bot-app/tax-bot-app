# AGENTS.md — さじかげん開発

## このリポジトリの目的

GLADZ → Vercelトップ → デモ → プラン → Stripe → Chat の導線を実装する。

## 変更方針

- 最小差分で変更し、不要なリファクタはしない。
- 仕様は `docs/00_憲法_確定仕様.md` に従う。
- 煽りに見える語をUIテキストへ入れない。
- スレッドで確定した内容は `docs/04_決定ログ.md` と正本へ反映する。

## GitHub更新の標準手順

- Project Filesだけを現行コードと見なさない。作業開始時に、GitHubの最新 `main` から取得したリポジトリZIP、または認証済みGitHub機能で確認した現行ファイルを使用する。
- 対象リポジトリ、ブランチ、取得時点が確認できない場合は、編集前に利用者へ最新版ZIPの添付を依頼する。
- 受領コードは作業用コピーで編集し、必要ファイルだけを元の相対階層のまま上書き用ZIPへまとめる。
- 利用者がGitHub Desktopで差分確認、Commit、Pushを行う。明示依頼がない限り、ChatGPTはGitHubへ直接Commit／Pushしない。
- 変更ファイル一覧、ファイル数、検証結果、反映順序、Commit Summaryを引渡し時に明記する。
- 想定外の既存変更、競合、対象外ファイルの差分が見つかった場合は、勝手に解消せず作業を止める。

## コード規約

- Next.js App Router前提。型安全・例外処理・リダイレクトループ回避を優先する。
- 秘密情報を出力・コミットしない。ENV名だけ記載可能。

## 確認手順

1. 対象ファイルと影響範囲を確認する。
2. `npm test` を実行する。
3. `npx tsc --noEmit` を実行する。
4. 変更対象のLintを実行し、既存違反と新規違反を分ける。
5. `npm run build` を実行する。
6. 差分、変更ファイル数、ZIP階層、秘密情報の混入がないことを確認する。
7. 変更点と本番確認手順を記録する。
